import buildCloneTemplateIntoPath from "./CloneTemplateIntoPathByName";

const CloneTemplateIntoPathByName = Object.freeze({
  build: buildCloneTemplateIntoPath,
});

export default CloneTemplateIntoPathByName;
